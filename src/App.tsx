import { Header } from '@components/header';
import { MobileNav } from '@components/MobileNav';

const App = () => {
  return (
    <div>
      <Header />
      <MobileNav />
    </div>
  );
};

export default App;
